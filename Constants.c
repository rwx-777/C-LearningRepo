#include <stdio.h>
#include <stdlib.h>

int main() {

    //constants cannot be modified can be helpful
    const int num = 5;

    //lots of people write varible names in full caps when defining constants
    const char NAME[] = 'Elliot';
    //or like this
    const int FAV_NUM = 44;
    

    printf("%d\n", num);

}